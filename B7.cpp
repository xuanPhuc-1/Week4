#include<iostream>
#include<string.h>
using namespace std;

int input(int n, string a) {
	string a1 ="";
	cin >> a;
	for (int i=a.length()-1; i>=0; i--)
        a1=a1+a[i];
    for (int i=1; i<=a.length()-1; i++)
	{
		if(a1 == a)
		}    
}
int main () {
	int n;
	cin >> n;
	string a;
	for(int i=1; i<=n; i++)
	{
		input(n,a);
	}
	
}